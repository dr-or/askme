<h1>All questions</h1>

<% @questions.each do |question| %>
	<p><%= link_to "Question #{question.id}", question_path(question) %></p>
<% end %>

<%= link_to 'Create new question', new_question_path %>
