<h2>Edit the question</h2>

<%= form_with model: @question do |form| %>
	<p><%= form.label :body, 'Your question:' %></p>
	<p><%= form.text_area :body, rows: 5 %></p>
	<p><%= form.label :user_id %></p>
	<p><%= form.text_area :user_id %></p>
	<p><%= form.submit %></p>
<% end %>
